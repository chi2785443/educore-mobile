appId: com.cakale.edu.app
---
# Reusable logout subflow. Call via `runFlow` after a smoke/deep flow finishes.
- tapOn: "Account"
- tapOn: "Sign Out"
- assertVisible:
    id: "email-input"
    timeout: 15000
