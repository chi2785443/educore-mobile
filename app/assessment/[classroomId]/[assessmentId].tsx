// Root-level assessment detail screen used by my-assessments and assessments.
// Keeping it in the root stack means router.back() naturally returns to the caller.
export { default } from '../../(tabs)/features/[classroomId]/assessment/[assessmentId]';
